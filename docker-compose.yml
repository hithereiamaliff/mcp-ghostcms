# Ghost CMS MCP Server - Docker Compose
# For self-hosting on VPS

services:
  mcp-ghostcms:
    build: .
    container_name: mcp-ghostcms
    restart: unless-stopped
    ports:
      - "8091:8080"
    environment:
      - PORT=8080
      - HOST=0.0.0.0
      - ANALYTICS_DIR=/app/data
      - NODE_ENV=production
      # MCP Key Service credentials
      - KEY_SERVICE_URL=${KEY_SERVICE_URL:-}
      - KEY_SERVICE_TOKEN=${KEY_SERVICE_TOKEN:-}
      - MCP_API_KEY=${MCP_API_KEY:-}
      - GHOST_API_VERSION=${GHOST_API_VERSION:-v5.0}
    volumes:
      - analytics-data:/app/data
      - firebase-credentials:/app/.credentials:ro
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:8080/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    networks:
      - mcp-network
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

networks:
  mcp-network:
    external: true

volumes:
  analytics-data:
    driver: local
  firebase-credentials:
    driver: local
